# Environment variables for VPS Instance 1 (Docker Services)
# This file should be used on the VPS running Docker containers

# MQTT Broker Configuration
# Use localhost since services are in same Docker network
MQTT_BROKER_HOST=mosquitto
MQTT_BROKER_PORT=1883

# InfluxDB Configuration
# Use localhost since services are in same Docker network
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=my-super-secret-auth-token
INFLUXDB_ORG=my-org
INFLUXDB_BUCKET=vehicle-data

# Flask Dashboard Configuration (if running collector on same VPS)
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

