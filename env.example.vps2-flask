# Environment variables for VPS Instance 2 (Flask Dashboard)
# This file should be used on the VPS running Flask dashboard

# MQTT Broker Configuration
# Use Cloudflare tunnel subdomain (e.g., mqtt.secruin.cloud)
MQTT_BROKER_HOST=mqtt.secruin.cloud
MQTT_BROKER_PORT=1883

# InfluxDB Configuration
# Use Cloudflare tunnel subdomain (e.g., influxdb.secruin.cloud)
INFLUXDB_URL=http://influxdb.secruin.cloud:8086
INFLUXDB_TOKEN=my-super-secret-auth-token
INFLUXDB_ORG=my-org
INFLUXDB_BUCKET=vehicle-data

# Flask Dashboard Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

